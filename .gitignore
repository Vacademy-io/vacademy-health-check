# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
branch_structure.json
temp_auto_push.bat
temp_interactive_push.bat

# Secrets. The AI-queue client signature belongs in .env.local (dev) and a
# Cloudflare Pages env binding (deployed) — never in a VITE_ var, which is
# inlined into the browser bundle.
.env
.env.*
!.env.example
